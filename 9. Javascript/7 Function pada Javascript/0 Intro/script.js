// Struktur Function
// fun(keyword)ction jumlahDua(nama function)Bilangan(a,(parameter) b) {
//     var total;
//     total = a + b;
            // (functio body)
//     return(return value) total;
// }

// Built-in function
// contoh
    // alert()
    // confirm()
    // prompt()

    // Built-in function: String
        // CharAt(), slice(), split(), toString(), toLowerCase(), toUpperCase(), ...

        // var nama = "Muhammad Ridho";
        // console.log(nama.charAt(5));
    
    // Built-in function:Math
        // sin(), cos(), tan(), random(), round(), floor(), ceil(), log() . ..

        // var bil = Math.random();
        // console.log(bil);
    
        // ref : w3school

// User-defined function
    // contoh

    // Function Declaration
        // function jumlahDuaBilangan(a, b) {
        //     var total;
        //     total = a + b;

        //     return total;
        // }

    // Function Expression
        // var jumlahDuaBilangan = function(a b) {
        //     var total;
        //     total = a + b;

        //     return total;
        // }

